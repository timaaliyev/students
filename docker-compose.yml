version: "3.8"
services:
    nodeserver:
        build:
            context: ./
        ports:
            - "7000:3000"
    nginx:
        restart: always
        build:
            context: ./client
        ports:
            - "3013:80"