version: "3.8"
services:
    nodeserver:
        build:
            context: ./
        image: ansiya/students/nodeserver
        ports:
            - "7000:3000"
    nginx:
        restart: always
        build:
            context: ./client
        image: ansiya/students/nginx
        ports:
            - "3013:80"